# Firebase CRUD App with Search & Sort
## Features
- Create, Read, Update, Delete (CRUD)
- Realtime update with onSnapshot
- Search and Sort on Bootstrap table

## Installation
```bash
npm install
npm run dev
